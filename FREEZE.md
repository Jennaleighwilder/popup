# Popup — Code Freeze

**Tag:** `v1.0-freeze`  
**Date:** February 28, 2026  
**Commit:** `c941195`

---

## What's frozen

- **Landing page** — Magazine editorial design reset (warm cream, temperature shifts, 10 sections)
- **10 themes** — Atelier, Harvest, Gallery, Botanica, Soirée, Brutalist, Zen, Maximalist, Neon, Vintage
- **Event pages** — `/e/[slug]` with demo data, theme switching
- **Auth** — Login, signup, Supabase
- **Create flow** — Category → details → generating → preview
- **Core components** — EventPreview, ThemeProvider, SectionDivider
- **Demo events** — Full dataset in `demoEvents.ts`

---

## Restore this version

```bash
git checkout v1.0-freeze
```

Or create a branch from it:

```bash
git checkout -b my-branch v1.0-freeze
```
